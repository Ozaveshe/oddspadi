node_modules
.next
.netlify
.playwright-cli
out
dist
coverage
output
*.log
*.tsbuildinfo
.env
.env*.local
supabase/.temp

# Expanded training corpora are extracted locally, never committed: the v2
# football odds CSV alone is 185MB. `training-data/standard-v1` predates this
# and stays tracked.
training-data/*
!training-data/standard-v1/

# local workspace/tooling artifacts
.codex
.codex-logs
.logs
logs
artifacts
.agents
.claude/settings.local.json
.claude/skills
dashboard-smoke.html
fixtures-smoke.json
materializer-smoke.json
predictions-smoke.json

# Delivered training corpus: large CSVs, tracked outside git.
training-data/
